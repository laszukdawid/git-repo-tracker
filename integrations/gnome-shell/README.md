# GNOME Shell Extension

This is the optional Ubuntu/Fedora GNOME frontend. It renders a rich panel menu
using GNOME Shell widgets and reads repo state from the headless Go CLI.

Development and install instructions live in
[`docs/GNOME_EXTENSION.md`](../../docs/GNOME_EXTENSION.md).
